name: Run Syft Scan

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    name: Generate SBOM
    steps:
    - name: Checkout repository content
      uses: actions/checkout@v3
    - name: Show file
      run: ls
      
